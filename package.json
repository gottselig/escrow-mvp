{
  "name": "escrow-mvp",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "pnpm install",
    "build": "pnpm --recursive build",
    "dev": "pnpm --recursive dev",
    "test": "pnpm --recursive test"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
